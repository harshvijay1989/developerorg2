<apex:page standardController="Document_Template__c" lightningStylesheets="true" recordSetVar="Document_Template__c">        
   <apex:includeLightning />
    <div id="LightningContainer" />
    <script>
    window.location.href = '/lightning/n/invoice_Creator';
    /*var parentRecId = "{!$CurrentPage.parameters.id}";
        $Lightning.use("c:invoiceCreatorAuraApp", function() {
            $Lightning.createComponent(
                "c:invoiceCreatorAuraCmp",
                {"parentRecId":parentRecId},
                "LightningContainer",
                function(component) {
                    console.log("Component is created!");
                    console.log(component);
                    console.log('@#@# '+parentRecId);
                });
            });   */     
      </script>
</apex:page>